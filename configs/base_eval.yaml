sampler:
  _target_: eval.rollout.Rollout
  config:
    max_episode_len: 100
    save_video: True
    num_samples: 2
    # prompt: open the sliding cabinet
    # video_prefix: prompting_sliding_cabinet
    prompt: null
    video_prefix: transformerBC
    save_dir: /data/anthony/gpt_transformer/logs
    exp_name: transformerBC_paired_and_lang_pred_done

env:
  _target_: envs.kitchen.KitchenEnv
  config:
    name: kitchen-mixed-v0  # kitchen-all-tasks-v0